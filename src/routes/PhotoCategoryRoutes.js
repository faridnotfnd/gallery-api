// import express from 'express';
// import * as PhotoCategoryController from '../controllers/PhotoCategoryController.js';

// const router = express.Router();

// // PhotoCategory routes
// router.post('/', PhotoCategoryController.assignCategoriesToPhoto);
// router.get('/photos/:photo_id', PhotoCategoryController.getPhotoCategories);
// router.get('/categories/:category_id', PhotoCategoryController.getPhotosByCategory);
// router.delete('/photos/:photo_id/categories/:category_id', PhotoCategoryController.removePhotoCategory);

// export default router;
